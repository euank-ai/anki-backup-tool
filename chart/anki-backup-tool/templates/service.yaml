apiVersion: v1
kind: Service
metadata:
  name: {{ include "anki-backup-tool.fullname" . }}
  labels:
    {{- include "anki-backup-tool.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "anki-backup-tool.selectorLabels" . | nindent 4 }}
